'use strict';
document.addEventListener('DOMContentLoaded', () => {
    // This is used for the global frontend for HFS (across all pages).
    // This might be useful in the future.
    // For now, everything is in a single speedtest.html.
});